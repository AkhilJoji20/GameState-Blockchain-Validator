# GameState-Blockchain-Validator
GameState Blockchain Validator is a prototype system that uses blockchain principles to detect tampering in offline game progress. Each game session is recorded as a hash-linked block, allowing the server to validate state transitions and automatically restore the last trusted state if manipulation is detected.
